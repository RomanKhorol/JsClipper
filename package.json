{
  "name": "js-clipper-monorepo",
  "version": "1.0.0",
  "packageManager": "npm@9.3.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "dependencies": {
    "classnames": "^2.5.1"
  }
}
